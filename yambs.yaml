# =====================================
# generator=datazen
# version=3.1.4
# hash=cead2e197d68b7c574c31c51fef9aa3c
# =====================================
---
project:
  name: pico

  github: {owner: &self vkottler}
  version: {major: 0, minor: 2, patch: 1}
variants:
  clang:
    suffix: &clang_version "-15"
    enabled: false

  # Disable regular targets.
  debug:
    enabled: false
  opt:
    enabled: false
  clang-opt:
    enabled: false

includes:
  - local/yambs.yaml
