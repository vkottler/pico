# =====================================
# generator=datazen
# version=3.1.4
# hash=e8f8f6ca55b6f5bfd3e41e309a47b870
# =====================================
---
project:
  name: pico

  github: {owner: &self vkottler}
  version: {major: 0, minor: 2, patch: 2}
variants:
  clang:
    suffix: &clang_version "-15"
    enabled: false

  # Disable regular targets.
  debug:
    enabled: false
  opt:
    enabled: false
  clang-opt:
    enabled: false

includes:
  - local/yambs.yaml
